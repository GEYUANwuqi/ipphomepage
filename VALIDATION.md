# 验证记录

本地验证日期：2026-09-14。

## 已通过

- `npm run build`：TypeScript 与 Vite 生产构建通过。
- `npm test`：26 项来源白名单 / 内容 schema / 领域逻辑 / HTTP / SQLite / 配色与圆形几何测试全部通过。
- `npm run test:e2e`：29 项 Playwright 测试通过；7 项移动项目下的重复矩阵、精确像素几何或鼠标专属测试明确跳过，相关检查已由桌面项目执行。
- 问卷五种题型、随机顺序、演示与正式评分、自动保存及刷新恢复、证书 PNG 下载、公开验真与撤销、管理员题目增改删、开启正式审核确认及退出登录均覆盖浏览器流程。
- 320、375、600、768、1024、1920px 下检查首页、问卷入口、验真与登录页横向溢出；修复了 320px 装饰插画导致的溢出。
- 4 个入口页面的浅色与深色模式经 axe WCAG 2 A/AA、2.1 AA 自动检查，无 serious / critical 级别问题。自动检查不等于完整人工无障碍认证。
- 正式证书 PNG 中的二维码经 jsQR 实际解码，与服务端配置来源及证书 UUID 对应的验真地址完全一致。人工查看中文证书截图，中文正常渲染。
- 人工查看桌面和手机首页截图，导航、服务卡片、品牌图形与内容层次正常。
- 使用真实生产构建、生产 Helmet CSP 与内存数据库启动 Node 服务；浏览器直接打开 `/`、`/assessment`、`/verify`、`/admin` 均成功，无 JavaScript 异常或 CSP violation。
- `npm audit`：0 已知漏洞。实现过程中将 Sharp 更新至修复相关 libvips / libheif 安全通告的版本。
- `docker compose config --quiet`：配置校验通过。
- 前端 dist 中未发现演示题库 ID 或抽查的标准答案文本；答题 API 的答案键 / 解析缺失由测试断言保证。

## 主题与动效补充验证

- 新增覆盖首页、成员、项目、赛事、问卷、结果、验真、管理后台和主题弹窗的 M3 Expressive 动效层。通用 surface、项目卡、书本、空状态、通知、统计区、后台行、证书预览等在精确鼠标悬停时提供 180–500ms 抬升、阴影、圆角或内部元素联动；非指针信息不依赖动效表达。
- 项目/成员/赛事筛选、问卷换题、后台 Tab、详情/通知/验真结果出现采用有限入场动画。问卷只重新挂载由服务端题目 ID 标识的受控题目内容，不重新挂载页面组件或丢失草稿；操作按钮保持静止，移动端两个完整 14 题证书流程通过。
- 路由入场在桌面为 500ms 位移、轻缩放、5px 模糊与圆角裁切；≤700px 保留 420ms 位移/缩放/裁切但取消全页模糊，降低移动 GPU 重绘。浏览器测试截获 WAAPI 帧与时长，不仅检查类名。
- 浏览器实际等待入场结束后，再断言赛事空状态、通知和通用卡片 hover 的 computed `translate` 生效；同时断言项目插画、人物头像、待施工牌、项目/成员筛选容器和筛选按钮 `translate`/`border-radius` transition。
- 原生 dialog 在支持 discrete transition 的浏览器中有 220–300ms 打开/关闭反馈；昼夜模式继续使用独立 680ms 圆形扩散，未被通用路由动画替代。
- 所有空间悬停严格置于 `(hover: hover) and (pointer: fine) and (prefers-reduced-motion: no-preference)`；触屏不模拟悬停。减少动态效果时路由、卡片、筛选、待施工牌、视差与状态弹性动画均停用，主题和表单状态仍可用。
- 独立只读动效审查发现的两个主要问题已修复：入场 `fill-mode: both` 曾锁住最终 `translate`，以及后加载的 `community.css` 曾覆盖筛选按钮 transition。复审未发现剩余 blocker/major；保留搜索框获取焦点时 2px 抬升，作为明确的输入聚焦反馈。
- 6 组预设种子色以及黑、白、荧光色等自定义极值均生成完整昼夜 M3 色彩角色；计入测试的文本/背景配对及扩展色对比度均不低于 4.5:1。
- 6 组预设 × 昼夜 2 模式，在主页与主题对话框进行 axe WCAG A/AA 检查，无 serious / critical 问题。
- 暂停真实根视图快照的圆形扩散动画，分别对昼→夜、夜→昼截图取像素：圈外像素与旧模式相同，圈内像素与最终新模式相同；圆心与按钮中心一致。不是仅检查 CSS 类名或模拟转场。
- 桌面鼠标悬停验证按钮空间位移、标题逐字动效、卡片图形展开；验证插画视差写入、离开后的复位，以及路由进入动画。
- 验证键盘触发、快速连续切换的最终一致性、主题持久化、非法颜色校验、原生颜色单选即时反馈、Escape 关闭及焦点返回。
- 模拟 reduced-motion、缺失/抛错的 View Transition API 和浏览器存储被禁止，实际主题仍可切换，表单内容不被重新挂载清空。
- 320 / 375 / 768px 下检查主题对话框宽度与内部无横向溢出。
- 使用生产构建和真实生产 CSP 验证动态样式注入、圆形转场、种子色与昼夜偏好跨页面恢复，无 JavaScript 错误或 CSP violation。

## 成员 / 项目 / 赛事改版验证

- `npm run check:content`：正式数据保持 **0 位成员、4 个项目、0 场赛事**；根目录参考照片不在 `public` 或生产资源中。
- 单元测试覆盖成员年级/组别/链接、唯一 ID、封面 alt、无效/倒置赛事日期、图片缺失、越界软链接、JSON 归一化、确定性 mock 与无链接/多链接边界。
- 六种子色的 I++ 三块牌在真实浏览器中比较 light/dark 的填充、文字、立体边缘；同种子下完全一致。所有 `--art-*` token 另有配对单元测试。待施工牌在同一种子色的昼夜模式下保持填充、前景和立体边缘一致；页面不再渲染人物 SVG。
- 书架支持名称/介绍搜索、组别与年级筛选、清空筛选、完整简介、书签溢出 dialog、Escape 与焦点返回。模拟图片请求失败可正常显示占位；生产临时夹具额外检查 80 字符姓名/身份、3000 字符介绍与 40 字符书签标签在 320/1440px 无横向或书签纵向溢出。
- 项目分类、Dora SSR 官方源码入口、Spine 提醒、《灵数奇缘》技术支持关系及赛事空状态由浏览器断言。
- `/`、`/people`、`/projects`、`/events` 在 320/375/768/1024/1440px 检查横向溢出；三个新页面在浅色/深色下经 axe 检查，无 serious / critical 问题。
- `npm run test:production`：扫描真实 dist 及临时夹具 dist，未发现开发 mock 模块、ID、名字或内联 SVG 标记；生产空书架/赛事状态真实，没有虚构名录。使用生产 Helmet CSP 打开四个内容页面并切换昼夜，无 JavaScript 异常或 CSP violation。
- 生产测试在临时副本中仅修改 JSON/本地头像即可显示成员与支持者、赛事日期、角色、外链与分类；测试结束删除副本，**不写入正式 people/events JSON**。
- 项目构建按 Material Color Utilities 拆出独立缓存块，主 JS 约 433KB / gzip 131KB，配色块约 96KB / gzip 20KB，无超 500KB 单块构建警告。
- 独立只读审查覆盖内容 schema/校验器、内容/provider/mock、三个新组件、People/Projects/Events 与部分测试，未发现阻塞项。它未完整覆盖 `Home/main/community.css/palette` 等文件，也未自行执行测试，不能当作全站独立安全审计。移除了头像组件对 mock 名称前缀的依赖，装饰占位与正常装饰头像采用一致读屏处理。
- SVG 图片只允许可信维护者发布，路径/存在性校验不等同 SVG 净化；已在 `CONTENT.md` 明确。爪印等 UI 点缀有意跟随界面角色色；固定画面与原图则不反色。保留简单的多次构建校验，避免为极小静态数据引入缓存失效风险。

## 本轮来源修复与人物撤回

- 已用真实 API 测试复现局域网 Origin 与默认 localhost 不一致造成的 403；新增明确的 `UNTRUSTED_ORIGIN` 错误码与开发配置提示。
- `dev.local.json` 只在非生产启动时加载，只接受显式 HTTP(S) 来源，不允许通配符、账号密码、路径、查询或片段。该文件从 Git/Docker 上下文排除，生产忽略所有开发来源例外。
- 测试覆盖正常主来源、显式局域网来源、其他 IP/端口/协议、null/缺失来源、源列表、伪造 Host 与代理头，以及生产拒绝开发例外；管理员密码检查继续生效。
- 未读取或更改现有 `.env`（读取被安全机制阻止）。本机开发白名单已写入用户提供的来源，需重启实际开发服务生效；没有改动生产来源或部署。
- 两轮人物 SVG、眨眼/视线跟随 hook、独立人物样式和试作截图已移除；首页猫咪符号/爪印保留。待施工牌以 CSS 与文本实现，已查看桌面近景和 320px 窄屏截图。
- 浏览器测试显式断言“站娘 · 待施工”可见，且人物/头部/视线跟随节点不存在；保留书架筛选、书签、主题稳定色、响应式及减少动态效果验证。

## 未验证 / 上线前事项

- 当前无运行中的 Docker daemon，未构建和运行最终容器镜像。
- 未修改 DNS、申请真实域名 TLS 或部署到 `iplusplus.club`；生产冒烟使用本地临时端口，不替代真实 HTTPS 链路验收。
- 正式题库尚待社团审核，默认仍是演示模式。
- 需要社团明确数据保留期限与隐私请求处理渠道，并验证备份恢复。
- 早期全站独立代码审查因配额失败未完成；本轮仅完成上述局部内容模块审查，未补做全站安全审计。
- 正式成员资料与赛事尚未录入。人物渲染已按用户要求撤回，原位置保留待施工牌；未来素材接入需重新确认，不在当前实施范围内。

## 截图

运行 Playwright 后，截图位于被 Git 忽略的 `test-results/`，包括桌面与手机首页浅色 / 深色、问卷结果、正式与演示证书、公开验真、后台设置、主题选择面板、成员书架、项目工坊、赛事空状态和两个方向的圆形转场中间帧。重新运行部分测试可能清除之前的结果，完整重建请执行 `npm run test:e2e`。
